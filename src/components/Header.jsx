
// eslint-disable-next-line react/prop-types
const Header = ( {datos} ) => {

    
  return (
    <div className='container-header'>
      <h1 className='title-header'>{datos.title}</h1>
    </div>
  )
}

export default Header

